Run the jar via command line using the following:

java -jar "eqconvert.jar" inFile outDirectory -s

inFile can be a file or a directory
-s is optional, and will result in the new files being in structured subdirectories (sitename/year/month/day/file)
